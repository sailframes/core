AWSTemplateFormatVersion: '2010-09-09'
Description: SailFrames Analytics Platform - Serverless Infrastructure

Parameters:
  Environment:
    Type: String
    Default: prod
    AllowedValues: [prod, staging]
    Description: Deployment environment

Resources:
  # ============================================
  # S3 BUCKETS
  # ============================================

  DataBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub 'sailframes-data-${Environment}'
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      CorsConfiguration:
        CorsRules:
          - AllowedHeaders: ['*']
            AllowedMethods: [GET, HEAD]
            AllowedOrigins: ['*']
            MaxAge: 3600
      LifecycleConfiguration:
        Rules:
          - Id: DeleteOldRawData
            Status: Enabled
            Prefix: raw/
            ExpirationInDays: 365
      # Note: S3 notifications configured via CLI post-deployment to avoid circular dependencies
      # aws s3api put-bucket-notification-configuration ...

  WebsiteBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub 'sailframes-web-${Environment}'
      WebsiteConfiguration:
        IndexDocument: index.html
        ErrorDocument: index.html
      PublicAccessBlockConfiguration:
        BlockPublicAcls: false
        BlockPublicPolicy: false
        IgnorePublicAcls: false
        RestrictPublicBuckets: false

  WebsiteBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref WebsiteBucket
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: PublicReadGetObject
            Effect: Allow
            Principal: '*'
            Action: s3:GetObject
            Resource: !Sub '${WebsiteBucket.Arn}/*'

  # ============================================
  # IAM ROLES
  # ============================================

  LambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub 'sailframes-lambda-${Environment}'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: S3Access
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:ListBucket
                Resource:
                  - !Sub 'arn:aws:s3:::sailframes-data-${Environment}'
                  - !Sub 'arn:aws:s3:::sailframes-data-${Environment}/*'

  PiUploaderUser:
    Type: AWS::IAM::User
    Properties:
      UserName: !Sub 'sailframes-pi-uploader-${Environment}'
      Policies:
        - PolicyName: S3UploadPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:PutObjectAcl
                Resource: !Sub 'arn:aws:s3:::sailframes-data-${Environment}/raw/*'
              - Effect: Allow
                Action: s3:ListBucket
                Resource: !Sub 'arn:aws:s3:::sailframes-data-${Environment}'

  # ============================================
  # LAMBDA FUNCTIONS
  # ============================================

  ProcessUploadFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub 'sailframes-process-upload-${Environment}'
      Runtime: python3.11
      Handler: handler.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 300
      MemorySize: 512
      Code:
        S3Bucket: !Sub 'sailframes-lambda-code-${Environment}'
        S3Key: process_upload.zip
      Environment:
        Variables:
          DATA_BUCKET: !Ref DataBucket

  ProcessUploadPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ProcessUploadFunction
      Action: lambda:InvokeFunction
      Principal: s3.amazonaws.com
      SourceAccount: !Ref AWS::AccountId
      SourceArn: !Sub 'arn:aws:s3:::sailframes-data-${Environment}'

  ApiSessionsFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub 'sailframes-api-sessions-${Environment}'
      Runtime: python3.11
      Handler: handler.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 30
      MemorySize: 256
      Code:
        S3Bucket: !Sub 'sailframes-lambda-code-${Environment}'
        S3Key: api_sessions.zip
      Environment:
        Variables:
          DATA_BUCKET: !Ref DataBucket

  ApiDataFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub 'sailframes-api-data-${Environment}'
      Runtime: python3.11
      Handler: handler.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 30
      MemorySize: 512
      Code:
        S3Bucket: !Sub 'sailframes-lambda-code-${Environment}'
        S3Key: api_data.zip
      Environment:
        Variables:
          DATA_BUCKET: !Ref DataBucket

  ApiVideoFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub 'sailframes-api-video-${Environment}'
      Runtime: python3.11
      Handler: handler.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 30
      MemorySize: 256
      Code:
        S3Bucket: !Sub 'sailframes-lambda-code-${Environment}'
        S3Key: api_video.zip
      Environment:
        Variables:
          DATA_BUCKET: !Ref DataBucket
          CLOUDFRONT_DOMAIN: !GetAtt WebsiteBucket.WebsiteURL

  # ============================================
  # VIDEO TRANSCODING
  # ============================================

  MediaConvertRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub 'sailframes-mediaconvert-${Environment}'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: mediaconvert.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: MediaConvertS3Access
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                Resource: !Sub '${DataBucket.Arn}/*'

  TranscodeVideoFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub 'sailframes-transcode-video-${Environment}'
      Runtime: python3.11
      Handler: handler.lambda_handler
      Role: !GetAtt TranscodeLambdaRole.Arn
      Timeout: 60
      MemorySize: 256
      Code:
        S3Bucket: !Sub 'sailframes-lambda-code-${Environment}'
        S3Key: transcode_video.zip
      Environment:
        Variables:
          DATA_BUCKET: !Ref DataBucket
          MEDIACONVERT_ROLE: !GetAtt MediaConvertRole.Arn
          MEDIACONVERT_ENDPOINT: !Sub 'https://mediaconvert.${AWS::Region}.amazonaws.com'
          CLOUDFRONT_DOMAIN: sailframes.com

  TranscodeCompleteFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub 'sailframes-transcode-complete-${Environment}'
      Runtime: python3.11
      Handler: handler.lambda_handler
      Role: !GetAtt LambdaExecutionRole.Arn
      Timeout: 120
      MemorySize: 512
      Code:
        S3Bucket: !Sub 'sailframes-lambda-code-${Environment}'
        S3Key: transcode_complete.zip
      Environment:
        Variables:
          DATA_BUCKET: !Ref DataBucket
          CLOUDFRONT_DOMAIN: sailframes.com

  TranscodeLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub 'sailframes-transcode-lambda-${Environment}'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: S3Access
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:PutObject
                  - s3:ListBucket
                Resource:
                  - !Sub 'arn:aws:s3:::sailframes-data-${Environment}'
                  - !Sub 'arn:aws:s3:::sailframes-data-${Environment}/*'
        - PolicyName: MediaConvertAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - mediaconvert:CreateJob
                  - mediaconvert:GetJob
                  - mediaconvert:DescribeEndpoints
                Resource: '*'
              - Effect: Allow
                Action: iam:PassRole
                Resource: !GetAtt MediaConvertRole.Arn

  TranscodeVideoPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref TranscodeVideoFunction
      Action: lambda:InvokeFunction
      Principal: s3.amazonaws.com
      SourceAccount: !Ref AWS::AccountId
      SourceArn: !Sub 'arn:aws:s3:::sailframes-data-${Environment}'

  # EventBridge rule to trigger on MediaConvert job completion
  MediaConvertCompleteRule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub 'sailframes-transcode-complete-${Environment}'
      Description: Trigger Lambda when MediaConvert job completes
      EventPattern:
        source:
          - aws.mediaconvert
        detail-type:
          - MediaConvert Job State Change
        detail:
          status:
            - COMPLETE
            - ERROR
      State: ENABLED
      Targets:
        - Id: TranscodeCompleteLambda
          Arn: !GetAtt TranscodeCompleteFunction.Arn

  MediaConvertCompletePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref TranscodeCompleteFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt MediaConvertCompleteRule.Arn

  # ============================================
  # API GATEWAY
  # ============================================

  ApiGateway:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: !Sub 'sailframes-api-${Environment}'
      ProtocolType: HTTP
      CorsConfiguration:
        AllowOrigins: ['*']
        AllowMethods: [GET, OPTIONS]
        AllowHeaders: ['*']
        MaxAge: 3600

  ApiStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      ApiId: !Ref ApiGateway
      StageName: '$default'
      AutoDeploy: true

  # Sessions endpoint
  SessionsIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref ApiGateway
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt ApiSessionsFunction.Arn
      PayloadFormatVersion: '2.0'

  SessionsRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref ApiGateway
      RouteKey: 'GET /api/sessions'
      Target: !Sub 'integrations/${SessionsIntegration}'

  SessionsPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ApiSessionsFunction
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${ApiGateway}/*'

  # Data endpoint
  DataIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref ApiGateway
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt ApiDataFunction.Arn
      PayloadFormatVersion: '2.0'

  DataRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref ApiGateway
      RouteKey: 'GET /api/data/{device_id}/{date}'
      Target: !Sub 'integrations/${DataIntegration}'

  DataPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ApiDataFunction
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${ApiGateway}/*'

  # Video endpoint
  VideoIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref ApiGateway
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt ApiVideoFunction.Arn
      PayloadFormatVersion: '2.0'

  VideoRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref ApiGateway
      RouteKey: 'GET /api/video/{device_id}/{date}'
      Target: !Sub 'integrations/${VideoIntegration}'

  VideoPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ApiVideoFunction
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${ApiGateway}/*'

Outputs:
  WebsiteURL:
    Description: S3 Website URL (use this until CloudFront is set up)
    Value: !GetAtt WebsiteBucket.WebsiteURL
    Export:
      Name: !Sub '${AWS::StackName}-WebsiteURL'

  DataBucketName:
    Description: S3 bucket for race data
    Value: !Ref DataBucket
    Export:
      Name: !Sub '${AWS::StackName}-DataBucket'

  WebsiteBucketName:
    Description: S3 bucket for website assets
    Value: !Ref WebsiteBucket
    Export:
      Name: !Sub '${AWS::StackName}-WebsiteBucket'

  ApiEndpoint:
    Description: API Gateway endpoint URL
    Value: !Sub 'https://${ApiGateway}.execute-api.${AWS::Region}.amazonaws.com'
    Export:
      Name: !Sub '${AWS::StackName}-ApiEndpoint'

  PiUploaderUser:
    Description: IAM user for Pi devices to upload data
    Value: !Ref PiUploaderUser
    Export:
      Name: !Sub '${AWS::StackName}-PiUploaderUser'
